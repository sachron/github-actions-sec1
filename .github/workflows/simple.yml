name: Workflow Shell Commands

on: [push, ]

jobs:
  run-shell-command:
    runs-on: unbuntu-latest
    steps:
      - name: echo a string
        run: echo "Hellow World"
      - name: multiline script
        run: |
          node -v
          npm -v
